pipeline {
    agent any

    stages {
        stage('validate') {
            steps {
                echo 'validate'
            }
        }
        stage('compile') {
            steps {
                echo 'compile'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploy'
            }
        }
        stage('test') {
            steps {
                echo 'test'
            }
        }
    }
}
